#!/usr/bin/sh

SCRIPT=$(readlink -f $0)
BASEDIR=$(dirname $SCRIPT)
BASEDIR=$(realpath "${BASEDIR}/..")

sudo sh "${BASEDIR}/scripts/bootstrap"

pushd "${BASEDIR}"
sudo git pull
popd

sudo sh "${BASEDIR}/scripts/bootstrap"
sudo apt-get update
sudo sh "${BASEDIR}/scripts/setup"
